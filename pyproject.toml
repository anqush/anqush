[project]
name = "anqush-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "anqush[dev]",
    "anqush-server",
]

[tool.uv.workspace]
members = ["sdk-python", "server"]

[tool.uv.sources]
anqush = { workspace = true }
anqush-server = { workspace = true }
